<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.HelloModulo4" parent="Theme.Material3.DayNight">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.HelloModulo4" parent="Base.Theme.HelloModulo4" />

    <style name="BlueTextView" parent="Widget.AppCompat.TextView">
        <item name="android:textColor">@color/custom_blue</item>
        <item name="android:fontFamily">@font/bernier_shade_regular</item>
        <item name="android:textAlignment">center</item>
        <item name="android:textSize">25sp</item>
    </style>
</resources>